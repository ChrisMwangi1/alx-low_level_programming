#ifndef _FUNCTION_LIKE_MACRO_H_
#define _FUNCTION_LIKE_MACRO_H_
/**
 * file - Creates a header file.
 * @x: input value
 * Description: A function-like macro ABS(x)
 * that computes the absolute value of a number x.
 */

#define ABS(x) ((x) < (0) ? -(1) * (x) : (x))

#endif
