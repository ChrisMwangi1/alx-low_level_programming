#include "main.h"
/**
 * _abs - Checks the absolute value of an integer argument n.
 * @n: integer
 * Return: absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
