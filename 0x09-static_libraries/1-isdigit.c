include "main.h"

/**
 * isdigit - Checks whether character argument is a digit.
 * @c: integer to check
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
