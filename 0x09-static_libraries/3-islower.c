#include "main.h"

/**
 * _islower - Checks that a character is typed in lowercase
 * @c: char to check
 * Return: 1 if lowercase, 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
